services:
  - type: web
    name: snippy-server
    env: docker
    dockerfilePath: server/Dockerfile
    envVars:
      - key: MONGO_URL
        sync: false
      - key: PORT
        value: 5000
    autoDeploy: true